# Netlify redirects file
# Permitir acesso direto aos arquivos XML (sitemap)
/sitemap.xml  /sitemap.xml  200
/*.xml        /*.xml        200

# Redireciona todas as outras solicitações para index.html para SPA funcionar
/*            /index.html   200
